# Changesets

This folder is used by [Changesets](https://github.com/changesets/changesets) to manage versioning and changelogs for `@alphaai/*` packages.

To record a change, run `pnpm changeset` from the repo root, follow the prompts, and commit the generated markdown file alongside your code changes.
